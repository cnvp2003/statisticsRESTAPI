package services



import com.google.inject.Inject
import model.Transaction
import org.specs2.matcher.Matchers


class StatisticsServiceSpecs @Inject() (statisticService: StatisticService) {
  //extends FlatSpec with Matchers {


/*  "getStatistic" should "return correct Statistics" in {
    statisticService.processTransaction(new Transaction(1.3, System.currentTimeMillis - 10000))
    statisticService.processTransaction(new Transaction(2.2, System.currentTimeMillis - 9000))
    statisticService.processTransaction(new Transaction(3.3, System.currentTimeMillis - 8000))
    statisticService.processTransaction(new Transaction(4.2, System.currentTimeMillis - 7000))
    statisticService.processTransaction(new Transaction(5.5, System.currentTimeMillis - 6000))
    statisticService.processTransaction(new Transaction(6.3, System.currentTimeMillis - 5000))
    statisticService.processTransaction(new Transaction(7.6, System.currentTimeMillis - 4000))
    statisticService.processTransaction(new Transaction(8.1, System.currentTimeMillis - 3000))
    statisticService.processTransaction(new Transaction(9.7, System.currentTimeMillis - 2000))
    statisticService.processTransaction(new Transaction(10.1, System.currentTimeMillis - 1000))

    val statistic = statisticService.getStatistic()
    assertEquals(statistic.count, 10l)
    assertEquals(statistic.sum, 151.3, 0.0)
    assertEquals(statistic.max, 65.5, 0.0)
    assertEquals(statistic.min, 2.8, 0.0)
    assertEquals(statistic.average, 151.3 / 10, 0.0)

  }*/
}


